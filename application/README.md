# Application Tier

This directory contains the application-tier files for the 3-Tier AWS infrastructure project.

- `submit.php` receives registration data and stores it in Amazon RDS MySQL.
- `index.html` provides a simple registration form.
- The application server is intended to remain in a private subnet.
